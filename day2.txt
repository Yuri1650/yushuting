# 元组,下标，从0开始编号
a = ()   #空的元祖

a = (1,2,3,4,"哈哈","嘻嘻",True,False)
print(a[4]),下标，从0开始编号

查找元组下标,index的用法
# 二维元组 
b =(a,"哈哈","嘻嘻")


# 数组
a =[]
元组一旦写好后不可修改，而数组可以修改

所有的方法都是小括号结尾，比如print()、input()
元组、数组、字典的取值，都是用中括号，比如a[0]

字典的特点
1、字典中的值没有顺序
2、字典的结构必须是 键值对的结构 key:value
3、字典的取值是通过key取value

数组和字典的删除
del a["name"]